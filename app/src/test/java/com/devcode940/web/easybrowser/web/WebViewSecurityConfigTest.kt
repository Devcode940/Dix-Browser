package com.devcode940.web.easybrowser.web

import org.junit.Assert.assertFalse
import org.junit.Test

/**
 * Basic unit test for WebViewSecurityConfig
 */
class WebViewSecurityConfigTest {

    @Test
    fun `should disable javascript by default`() {
        // This is a placeholder test
        // In real implementation, we would test the config
        assertFalse(false)
    }
}